/*
    BOJ 10573 증가하는 수
    https://www.acmicpc.net/problem/10573
*/

#include <algorithm>
#include <iostream>
#include <vector>
typedef long long ll;

using namespace std;

int T;
vector<ll> vec;

int main() {
    cin.tie(0);
    ios_base::sync_with_stdio(0);

    cin >> T;

    int num;

    for (int i = 0; i < T; i++) {
        cin >> num;
        if (num < 10) {
        }
    }

    return 0;
}